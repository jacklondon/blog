{% extends "base.html.twig" %}
{% block body %}
    <div class="container">

    </div>
    {{ form_errors(form) }}
    {{ form_start(form) }}
    <div class="form-group-lg">
        {{ form_row(form.title) }}
    </div>
    <div class="form-group">
        {{ form_row(form.content) }}
    </div>

    {{ form_end(form) }}


{% endblock %}

